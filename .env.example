# AI Auto Trade — environment variable reference
# Copy to .env for local development. NEVER commit .env
# All values here are PLACEHOLDERS — no real credentials

# Database (Task 0.3)
# DATABASE_URL=postgresql://user:password@localhost:5432/ai_auto_trade

# Application
# LOG_LEVEL=INFO
# ENVIRONMENT=local
