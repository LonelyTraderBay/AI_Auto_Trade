# Task evidence

Store immutable task evidence under `docs/governance/evidence/tasks/<TASK_ID>/`. Evidence must contain exact command/procedure, UTC time, actor, result and artifact hash/path. No task in Phase 0.0 is considered DONE until its evidence is reviewed.
